{
  "name": "@bbom/bzpos-login_module-bbom",
  "version": "0.1.0",
  "description": "Pilot BBOM login module (ports-first)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "fastify": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "@types/node": "^20.0.0"
  }
}